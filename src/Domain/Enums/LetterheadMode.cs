namespace SchoolBehaviorSystem.Domain.Enums;

public enum LetterheadMode
{
    Text,   // نصية
    Image   // صورة
}

public enum WhatsAppMode
{
    PerStage,   // رقم لكل مرحلة
    Unified     // رقم موحد
}
